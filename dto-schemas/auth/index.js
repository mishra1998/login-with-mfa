const register = require('./register');
const verification = require('./verification');
const setPassword = require('./set-password');
const login = require('./login');

module.exports = {
  register, verification, setPassword, login,
};
